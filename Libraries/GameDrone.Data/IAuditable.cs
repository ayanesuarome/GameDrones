namespace GameDrones.Data
{
    /// <summary>
    /// Shadow definition. All the entities which will be auditable must implement it
    /// for the loaded entities, and can be used when inserting or updating records.
    /// </summary>
    public interface IAuditable
    {
    }
}
